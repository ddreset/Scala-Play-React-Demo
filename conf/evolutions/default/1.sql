# --- !Ups

CREATE TABLE "CLUB" ("ID" BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY,"NAME" VARCHAR NOT NULL, "ABBR" VARCHAR);
create TABLE "MEMBER" ("ID" BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY,"NAME" VARCHAR NOT NULL, "CLUBID" BIGINT NOT NULL);

# --- !Downs

DROP TABLE "CLUB" IF EXISTS;
DROP TABLE "MEMBER" IF EXISTS;